
<h1>The todo tasks are:</h1>

<%= link_to "Create Todo", new_task_path %>

<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <%= link_to "Show more", task_path(task) %>
  <!-- task_path(task) == task_path(id: task.id) -->
<% end %>
